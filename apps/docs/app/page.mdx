import { Callout } from 'nextra/components';

# Answerfox

> The drop-in SEO toolkit that makes any site answerable by AI search engines.

Install a few packages, run one command, and your Next.js site is SEO-ready, AI-search-ready, and audited continuously — without you becoming an SEO expert.

```bash
# In an existing Next.js project:
pnpm add @answerfox/schemas @answerfox/metadata @answerfox/sitemap @answerfox/audit

# Scaffold trust pages + sitemap + robots:
pnpm dlx @answerfox/cli init

# Audit the result against 33 production-tested checks:
pnpm dlx @answerfox/cli audit http://localhost:3000
```

A score of 85+ on first audit is the design target. The [`examples/basic-nextjs`](https://github.com/Anuj7411/answerfox/tree/main/examples/basic-nextjs) example scores 90+ out of the box.

<Callout type="info">
  **v0.1.0 is live on npm** under the [`@answerfox/*`](https://www.npmjs.com/org/answerfox) scope. Currently shipping 33 of 50 audit checks — the remaining 17 land incrementally before v1.0.
</Callout>

## What's in the toolkit

| Package | Purpose |
| --- | --- |
| `@answerfox/core` | Branded URL types, errors, the `Check<T>` interface |
| `@answerfox/schemas` | Type-safe JSON-LD generators (8 of them) |
| `@answerfox/metadata` | `defineSeo()` — Next.js metadata helpers |
| `@answerfox/sitemap` | `buildSitemap()` with smart path-based defaults |
| `@answerfox/templates` | Five trust-signal page templates |
| `@answerfox/audit` | Cheerio-backed audit engine (33 of 50 checks) |
| `@answerfox/cli` | The `answerfox` command |

## Why Answerfox?

Indie developers and SaaS founders waste **20–40 hours per launch** on SEO that should be automated. No other tool combines drop-in code + automated audit + plain-English explanations in one MIT-licensed package — that's the wedge.

| | Drop-in fixes | Continuous audit | Plain-English teaching |
| :--- | :---: | :---: | :---: |
| `next-seo` | partial | ❌ | ❌ |
| Lighthouse | ❌ | partial | ❌ |
| Ahrefs / Semrush | ❌ | ✅ | ❌ |
| AI-SEO checklists | ❌ | partial | ✅ |
| **Answerfox** | ✅ | ✅ | ✅ |

## Next steps

- **[Getting Started →](/docs)** — The 30-second story, in detail
- **[The audit framework →](/docs/audit-framework)** — How scoring works, what the bands mean
- **[All audit checks →](/docs/checks/A1)** — Every check, with rationale and fix recommendation

## Recipes

Short, problem-shaped solutions:

- **[How to add FAQ schema in 60 seconds →](/docs/recipes/add-faq-schema)**
- **[How to fix a missing canonical URL →](/docs/recipes/fix-missing-canonical)**
- **[How to wire Answerfox into CI →](/docs/recipes/ci-integration)**

## API reference

One page per package, listing every public symbol with examples:

- [`@answerfox/core`](/docs/reference/core) — branded URL types, errors, `Check<T>` interface
- [`@answerfox/schemas`](/docs/reference/schemas) — JSON-LD generators (8 of them)
- [`@answerfox/metadata`](/docs/reference/metadata) — `defineSeo()` for Next.js
- [`@answerfox/sitemap`](/docs/reference/sitemap) — smart-default sitemap builder
- [`@answerfox/templates`](/docs/reference/templates) — page-template registry
- [`@answerfox/audit`](/docs/reference/audit) — audit engine + reporter
- [`@answerfox/cli`](/docs/reference/cli) — the `answerfox` command

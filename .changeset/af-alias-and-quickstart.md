---
"@answerfox/cli": patch
---

Add `af` short alias for the `answerfox` binary. After `pnpm add -g @answerfox/cli`, both `answerfox audit <url>` and `af audit <url>` work identically. Saves 7 characters per invocation, faster for repeat use.
